extends layout

block content
  h1= title
  p Welcome to #{title} My favorite hobby is to watch anime series
  button( class ="btn btn-success" onclick="goFaster()") Click me to go faster
  br 
  br
  div(id="faster") Increase the speed
  a(href="/widget") View widget page
  script.
      var mph = 20;
      var change = 1;

      function goFaster() {
        mph += change;
        change += 1;
        document.getElementById("faster").innerHTML = `MPH ${mph} with change ${change}`;
      }
